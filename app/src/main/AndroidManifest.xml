<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.travelapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_out_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_out_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelApp">
        <activity android:name=".Destination"
            android:label="Destination"
            android:theme="@style/DemoTheme"></activity>
        <activity
            android:name=".Home"
            android:theme="@style/DemoTheme" />
        <activity
            android:name=".SignIn"
            android:theme="@style/DemoTheme" />
        <activity
            android:name=".Show_travilsActivity"
            android:theme="@style/DemoTheme" />
        <activity
            android:name=".SignUp"
            android:theme="@style/DemoTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/DemoTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>